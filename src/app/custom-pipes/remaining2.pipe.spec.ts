import { Remaining2Pipe } from './remaining2.pipe';

describe('Remaining2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Remaining2Pipe();
    expect(pipe).toBeTruthy();
  });
});
